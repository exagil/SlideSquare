<div class="col-md-9">
  <h2><%= @tag.name %></h2>
  <div class="col-md-12">
    <% @presentations.each do |presentation| %>
      <h5><%= link_to presentation.title, presentation_path(presentation) %></h5>
    <% end %>
  </div>
</div>